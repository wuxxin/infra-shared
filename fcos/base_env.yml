# Butane Transpiler default environment
DEBUG: false                  # true/false
UPDATE_SERVICE_STATUS: true   # true/false
CONTAINER_FRONTEND: true      # true/false
# FRONTEND_DASHBOARD: None, set to string of full hostname
#   eg. `traefik.host.domain` to enable traefik dashboard
DNS_RESOLVER: true            # true/false
# other DNS_ options, see docs/dnsresolver.md
INTERNAL_CIDR: 10.87.250.1/24
PODMAN_CIDR: 10.88.0.0/16
RPM_OSTREE_INSTALL:
  - docker-compose
  - podman-compose
  - systemd-networkd
LOCALE:
  LANG: en_US.UTF-8
  KEYMAP: us
  TIMEZONE: UTC
  COUNTRY_CODE: UN

